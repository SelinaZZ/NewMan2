For linux and OS
How to run the program:

1. javac -cp ./*:. Sampling.java
2. java -cp ./*:. Sampling abalone.data

If you want to test other data file, follow the below pattern:
2. java -cp ./*:. Sampling <filename>

Test the algorithm:

With the same size of data set, more percent in miscatogorization leads to lower accuracy in prediction.
The accuracy is directively proportionally to the size of data set.
The larger the data, the more closer to predict labels.
